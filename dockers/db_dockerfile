FROM mysql:latest
WORKDIR /app
RUN echo "NEW FILE"

COPY db/ /app/
RUN ls /app
RUN ls /app/
RUN ls app/

ENV MYSQL_ROOT_PASSWORD = Hamza#410
ADD app/db/user.sql /docker-entrypoint-initdb.d/

EXPOSE 3306
